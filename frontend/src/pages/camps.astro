---
import MainLayout from '../layouts/MainLayout.astro';
import { holidayTrips } from '../lib/holidayData';
---

<MainLayout title="Camps & Travel | Die Brücke">
    
    <div class="home-content px-4 md:px-6">
        
        {/* --- HERO SECTION --- */}
        <section class="relative z-10 px-6 sm:px-10 pt-32 pb-32 bg-white rounded-t-[32px] md:rounded-t-[64px]">
            <div class="max-w-7xl mx-auto relative z-10">
                
                {/* BACKLINK STIL WIE IN SLUG.ASTRO */}
                <div class="mb-12">
                    <a href="/services" class="inline-flex items-center text-slate-500 hover:text-slate-900 font-bold transition-colors group text-sm uppercase tracking-widest">
                        <i class="fa-solid fa-arrow-left mr-2 group-hover:-translate-x-1 transition-transform"></i>
                        Zurück zur Übersicht
                    </a>
                </div>

                <div class="max-w-6xl">
                    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-slate-50 border border-slate-200 text-xs font-bold text-rose-500 mb-8 uppercase tracking-widest shadow-sm">
                        <span class="text-[10px] mb-0.5">●</span> Escape the Ordinary
                    </div>
                    
                    <h1 class="text-6xl md:text-8xl font-display font-extrabold text-slate-900 tracking-tighter leading-[0.9] mb-12">
                        Camps & <br>
                        <span class="text-transparent bg-clip-text bg-gradient-to-r from-rose-500 via-indigo-500 to-indigo-600">Travel.</span>
                    </h1>
                    
                    <p class="text-xl text-slate-500 max-w-2xl leading-relaxed border-l-4 border-rose-500 pl-8 font-medium">
                        Tapetenwechsel gefällig? Unsere betreuten Camps bieten professionelle Begleitung, barrierefreie Ziele und unvergessliche Momente in der Gruppe.
                    </p>
                </div>
            </div>
        </section>

        {/* --- CAMPS GRID --- */}
        <section class="px-6 sm:px-10 pb-32 bg-white">
            <div class="max-w-7xl mx-auto">
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {holidayTrips.map((trip) => (
                        <div class="group relative bg-slate-50 rounded-[40px] overflow-hidden border border-slate-100 hover:shadow-2xl transition-all duration-500 flex flex-col h-full">
                            <div class="relative aspect-[4/3] overflow-hidden">
                                <img src={trip.img} alt={trip.title} class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" />
                                <div class="absolute top-6 left-6 flex gap-2">
                                    {trip.tags.map(tag => (
                                        <span class="px-3 py-1 bg-white/90 backdrop-blur-md rounded-full text-[10px] font-bold uppercase tracking-wider text-slate-900 shadow-sm">{tag}</span>
                                    ))}
                                </div>
                            </div>
                            
                            <div class="p-8 flex flex-col flex-grow">
                                <div class="flex justify-between items-start mb-4">
                                    <div>
                                        <p class="text-rose-500 font-bold text-xs uppercase tracking-widest mb-1">{trip.date}</p>
                                        <h3 class="text-2xl font-display font-bold text-slate-900 leading-tight tracking-tight">{trip.title}</h3>
                                    </div>
                                    <div class="w-12 h-12 rounded-2xl bg-white border border-slate-100 flex items-center justify-center text-rose-500 shadow-sm">
                                        <i class="fa-solid fa-plane-departure"></i>
                                    </div>
                                </div>
                                <p class="text-slate-500 text-sm leading-relaxed mb-8 flex-grow">
                                    {trip.description}
                                </p>
                                <div class="flex items-center justify-between pt-6 border-t border-slate-200/60 mt-auto">
                                    <span class="text-slate-400 text-xs font-bold uppercase tracking-widest">{trip.location}</span>
                                    <a href="/contact" class="text-slate-900 font-bold text-sm flex items-center gap-2 group/link">
                                        Anfragen <i class="fa-solid fa-arrow-right text-rose-500 group-hover/link:translate-x-1 transition-transform"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    ))}

                    {/* SUGGESTION CARD */}
                    <div class="bg-rose-600 rounded-[40px] p-10 text-white flex flex-col justify-center items-center text-center group shadow-xl">
                        <div class="w-20 h-20 rounded-full bg-white/10 flex items-center justify-center text-3xl mb-8 group-hover:scale-110 transition-transform">
                            <i class="fa-solid fa-map-location-dot"></i>
                        </div>
                        <h3 class="text-3xl font-display font-bold mb-4 tracking-tight">Eigene Idee?</h3>
                        <p class="text-rose-100 mb-8 leading-relaxed">Du hast ein Ziel im Kopf, das wir unbedingt mal ansteuern sollten? Lass es uns wissen!</p>
                        <a href="/contact" class="bg-white text-rose-600 px-8 py-3 rounded-full font-bold hover:bg-slate-900 hover:text-white transition-all">Vorschlag schicken</a>
                    </div>
                </div>
            </div>
        </section>

        {/* --- INFO / FAQ SECTION --- */}
        <section class="mb-12 rounded-[48px] px-6 sm:px-10 py-24 bg-slate-900 text-white relative overflow-hidden shadow-2xl">
            <div class="max-w-7xl mx-auto relative z-10">
                <div class="grid lg:grid-cols-2 gap-20 items-center">
                    <div>
                        <span class="text-rose-500 font-bold tracking-widest uppercase text-xs mb-6 block tracking-[0.2em]">Safe Travels</span>
                        <h2 class="text-4xl md:text-5xl font-display font-extrabold mb-8 leading-tight tracking-tighter">
                            Inklusion endet <br><span class="italic text-slate-400">nicht an der Grenze.</span>
                        </h2>
                        <ul class="space-y-6">
                            {[
                                { t: "Professionelle Teams", d: "Erfahrene Begleiter:innen sind rund um die Uhr für dich da." },
                                { t: "Geprüfte Barrierefreiheit", d: "Wir checken jedes Hotel und jeden Weg vorab persönlich." },
                                { t: "Individuelle Unterstützung", d: "Wir passen uns deinem Tempo und deinen Bedürfnissen an." }
                            ].map(item => (
                                <li class="flex gap-4">
                                    <div class="w-6 h-6 rounded-full bg-rose-500 shrink-0 flex items-center justify-center text-[10px] mt-1">
                                        <i class="fa-solid fa-check"></i>
                                    </div>
                                    <div>
                                        <strong class="block text-lg mb-1 font-bold">{item.t}</strong>
                                        <p class="text-slate-400 text-sm">{item.d}</p>
                                    </div>
                                </li>
                            ))}
                        </ul>
                    </div>
                    <div class="bg-white/5 p-12 rounded-[48px] border border-white/10 backdrop-blur-sm">
                        <h3 class="text-2xl font-bold mb-6">Wichtig zu wissen</h3>
                        <p class="text-slate-400 mb-8 leading-relaxed italic">
                            „Die Anmeldung für die Sommerreisen startet meist im Frühjahr. Da die Plätze begrenzt sind, empfehlen wir eine frühzeitige Anfrage.“
                        </p>
                        <div class="space-y-4">
                            {/* GEÄNDERTER BUTTON TEXT & STIL */}
                            <a href="/contact" class="flex items-center justify-between p-6 bg-white/5 rounded-3xl hover:bg-white/10 transition-all border border-white/5 group">
                                <span class="font-bold text-lg">Noch Fragen? Wir sind für dich da.</span>
                                <i class="fa-solid fa-chevron-right text-rose-500 group-hover:translate-x-1 transition-transform"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </section>

    </div>
</MainLayout>