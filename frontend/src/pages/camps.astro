---
import MainLayout from '../layouts/layout.astro';
import { holidayTrips } from '../lib/holidayData';

// LOGIK: Wir simulieren die "Featured Checkbox" aus Directus.
// Aktuell nehmen wir einfach das ERSTE Element als Highlight.
const tripsWithLayout = holidayTrips.map((trip, index) => ({
  ...trip,
  // Später kommt das aus der DB: trip.is_featured
  isFeatured: index === 0 
}));
---

<MainLayout title="Camps & Travel | Die Brücke">
    
    {/* --- HERO SECTION (Studio Dark Style) --- */}
    <section class="px-4 sm:px-6 pt-6 pb-20">
        <div class="relative bg-slate-900 rounded-[48px] overflow-hidden min-h-[60vh] flex items-center px-6 md:px-16 py-20">
            
            {/* Background Effects */}
            <div class="absolute top-0 right-0 w-full md:w-2/3 h-full overflow-hidden pointer-events-none">
                <div class="absolute top-[-20%] right-[-10%] w-[700px] h-[700px] bg-rose-500/20 rounded-full blur-[120px] animate-pulse-slow"></div>
                <div class="absolute bottom-[-10%] right-[20%] w-[500px] h-[500px] bg-indigo-500/10 rounded-full blur-[100px]"></div>
                <div class="absolute inset-0 bg-[url('/noise.png')] opacity-20 mix-blend-overlay"></div>
            </div>

            <div class="max-w-7xl mx-auto w-full relative z-10 grid lg:grid-cols-2 gap-16 items-center">
                
                {/* Text Content */}
                <div>
                    <div class="mb-8">
                        <a href="/services" class="inline-flex items-center text-slate-400 hover:text-white font-bold transition-colors group text-xs uppercase tracking-widest">
                            <i class="fa-solid fa-arrow-left mr-2 group-hover:-translate-x-1 transition-transform"></i>
                            Zurück zur Übersicht
                        </a>
                    </div>

                    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/5 border border-white/10 text-rose-300 text-xs font-bold uppercase tracking-widest mb-8 backdrop-blur-md">
                        <span class="w-2 h-2 rounded-full bg-rose-500 animate-pulse"></span>
                        Escape the Ordinary
                    </div>
                    
                    <h1 class="text-6xl md:text-8xl font-display font-bold text-white mb-8 leading-[0.95] tracking-tight">
                        Camps & <br/>
                        <span class="text-transparent bg-clip-text bg-gradient-to-r from-rose-400 to-indigo-400">Travel.</span>
                    </h1>
                    
                    <p class="text-xl text-slate-300 leading-relaxed mb-10 max-w-lg font-medium border-l-4 border-rose-500 pl-6">
                        Tapetenwechsel gefällig? Unsere betreuten Camps bieten professionelle Begleitung, barrierefreie Ziele und unvergessliche Momente.
                    </p>
                    
                    <div class="flex flex-wrap gap-4">
                        <a href="#camps" class="px-8 py-3 bg-white text-slate-900 rounded-full font-bold hover:bg-rose-500 hover:text-white transition-all shadow-lg hover:shadow-rose-500/20">
                            Reiseziele ansehen
                        </a>
                    </div>
                </div>

                {/* Hero Graphic / Image */}
                <div class="relative hidden lg:block perspective-1000">
                    <div class="relative z-10 rounded-[48px] overflow-hidden shadow-2xl rotate-1 hover:rotate-0 transition-transform duration-700 border border-white/10 bg-slate-800 aspect-square">
                        <img 
                            src="https://images.unsplash.com/photo-1469854523086-cc02fe5d8800?q=80&w=1000&auto=format&fit=crop" 
                            alt="Reisen" 
                            class="w-full h-full object-cover opacity-90 hover:opacity-100 transition-opacity duration-700"
                        />
                         <div class="absolute inset-0 bg-gradient-to-t from-slate-900/60 to-transparent pointer-events-none"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    {/* --- CAMPS GRID (Bento) --- */}
    <section id="camps" class="px-4 sm:px-6 pb-40">
        <div class="max-w-7xl mx-auto">
            
            {/* Grid Definition: dense flow füllt Lücken */}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 grid-flow-dense">
                
                {tripsWithLayout.map((trip) => (
                    <div class={`
                        camp-card group relative rounded-[40px] overflow-hidden border border-slate-100 transition-all duration-500 flex flex-col shadow-xl hover:shadow-2xl hover:-translate-y-2 bg-white
                        ${trip.isFeatured ? 'md:col-span-2 md:row-span-2 min-h-[500px] md:min-h-[600px]' : 'col-span-1 min-h-[450px]'}
                    `}>
                        
                        {/* IMAGE SECTION */}
                        <div class={`relative overflow-hidden ${trip.isFeatured ? 'h-3/5' : 'h-1/2'}`}>
                            <img src={trip.img} alt={trip.title} class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-[1.5s]" />
                            <div class="absolute inset-0 bg-gradient-to-t from-slate-900/60 to-transparent opacity-60"></div>
                            
                            {/* Tags on Image */}
                            <div class="absolute top-6 left-6 flex flex-wrap gap-2">
                                {trip.isFeatured && (
                                    <span class="px-3 py-1 bg-rose-500 text-white rounded-full text-[10px] font-bold uppercase tracking-wider shadow-lg animate-pulse">
                                        Highlight
                                    </span>
                                )}
                                {trip.tags.map(tag => (
                                    <span class="px-3 py-1 bg-white/20 backdrop-blur-md border border-white/20 rounded-full text-[10px] font-bold uppercase tracking-wider text-white shadow-sm">
                                        {tag}
                                    </span>
                                ))}
                            </div>
                        </div>
                        
                        {/* CONTENT SECTION */}
                        <div class="p-8 flex flex-col flex-grow relative z-10">
                            <div class="flex justify-between items-start mb-4">
                                <div>
                                    <p class="text-rose-500 font-bold text-xs uppercase tracking-widest mb-2 flex items-center gap-2">
                                        <i class="fa-regular fa-calendar"></i> {trip.date}
                                    </p>
                                    {/* Titel Größe dynamisch */}
                                    <h3 class={`camp-title font-display font-bold text-slate-900 leading-tight tracking-tight ${trip.isFeatured ? 'text-4xl md:text-5xl mb-4' : 'text-2xl mb-2'}`}>
                                        {trip.title}
                                    </h3>
                                </div>
                                
                                {trip.isFeatured && (
                                    <div class="w-16 h-16 rounded-2xl bg-slate-50 dark:bg-slate-800 flex items-center justify-center text-rose-500 text-2xl shadow-sm">
                                        <i class="fa-solid fa-star"></i>
                                    </div>
                                )}
                            </div>
                            
                            <p class={`camp-desc text-slate-500 leading-relaxed mb-8 flex-grow ${trip.isFeatured ? 'text-lg max-w-xl' : 'text-sm'}`}>
                                {trip.description}
                            </p>
                            
                            <div class="flex items-center justify-between pt-6 border-t border-slate-100 dark:border-white/10 mt-auto">
                                <span class="camp-loc text-slate-400 text-xs font-bold uppercase tracking-widest flex items-center gap-2">
                                    <i class="fa-solid fa-location-dot"></i> {trip.location}
                                </span>
                                <a href="/contact" class="camp-link text-slate-900 font-bold text-sm flex items-center gap-2 group/link px-4 py-2 rounded-full hover:bg-slate-100 dark:hover:bg-white/10 transition-colors">
                                    Anfragen <i class="fa-solid fa-arrow-right text-rose-500 group-hover/link:translate-x-1 transition-transform"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                ))}

                {/* SUGGESTION CARD (Bento Style) */}
                <div class="bg-rose-600 rounded-[40px] p-10 text-white flex flex-col justify-center items-center text-center group shadow-xl relative overflow-hidden">
                    <div class="absolute inset-0 bg-[url('/noise.png')] opacity-20 mix-blend-multiply"></div>
                    <div class="relative z-10">
                        <div class="w-20 h-20 rounded-full bg-white/20 backdrop-blur flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform shadow-lg border border-white/20">
                            <i class="fa-solid fa-paper-plane"></i>
                        </div>
                        <h3 class="text-3xl font-display font-bold mb-4 tracking-tight">Eigene Idee?</h3>
                        <p class="text-rose-100 mb-8 leading-relaxed font-medium">Du hast ein Traumziel im Kopf? Lass es uns wissen, wir planen gerne Neues!</p>
                        <a href="/contact" class="bg-white text-rose-600 px-8 py-3 rounded-full font-bold hover:bg-slate-900 hover:text-white transition-all shadow-lg inline-block">
                            Vorschlag schicken
                        </a>
                    </div>
                </div>

            </div>
        </div>
    </section>

    {/* --- INFO / FAQ SECTION --- */}
    <section class="px-4 sm:px-6 pb-24">
        <div class="max-w-7xl mx-auto rounded-[48px] px-8 py-20 bg-slate-50 dark:bg-slate-900/50 border border-slate-100 dark:border-slate-800 relative overflow-hidden">
            <div class="grid lg:grid-cols-2 gap-16 items-center relative z-10">
                <div>
                    <span class="text-rose-500 font-bold tracking-widest uppercase text-xs mb-6 block tracking-[0.2em]">Safe Travels</span>
                    <h2 class="text-4xl md:text-5xl font-display font-extrabold mb-8 leading-tight tracking-tighter text-slate-900 dark:text-white">
                        Inklusion endet <br/><span class="italic text-slate-400">nicht an der Grenze.</span>
                    </h2>
                    <ul class="space-y-6">
                        {[
                            { t: "Professionelle Teams", d: "Erfahrene Begleiter:innen sind rund um die Uhr für dich da." },
                            { t: "Geprüfte Barrierefreiheit", d: "Wir checken jedes Hotel und jeden Weg vorab persönlich." },
                            { t: "Individuelle Unterstützung", d: "Wir passen uns deinem Tempo und deinen Bedürfnissen an." }
                        ].map(item => (
                            <li class="flex gap-4">
                                <div class="w-6 h-6 rounded-full bg-rose-500 shrink-0 flex items-center justify-center text-[10px] mt-1 text-white shadow-md">
                                    <i class="fa-solid fa-check"></i>
                                </div>
                                <div>
                                    <strong class="block text-lg mb-1 font-bold text-slate-900 dark:text-white">{item.t}</strong>
                                    <p class="text-slate-500 dark:text-slate-400 text-sm">{item.d}</p>
                                </div>
                            </li>
                        ))}
                    </ul>
                </div>
                
                <div class="bg-white dark:bg-slate-800 p-10 rounded-[40px] shadow-xl border border-slate-100 dark:border-slate-700">
                    <h3 class="text-2xl font-bold mb-6 text-slate-900 dark:text-white">Wichtig zu wissen</h3>
                    <p class="text-slate-500 dark:text-slate-300 mb-8 leading-relaxed italic text-lg">
                        „Die Anmeldung für die Sommerreisen startet meist im Frühjahr. Da die Plätze begrenzt sind, empfehlen wir eine frühzeitige Anfrage.“
                    </p>
                    <a href="/contact" class="flex items-center justify-between p-5 bg-slate-50 dark:bg-slate-900 rounded-3xl hover:bg-slate-100 dark:hover:bg-slate-950 transition-all border border-slate-200 dark:border-slate-700 group">
                        <span class="font-bold text-slate-900 dark:text-white">Noch Fragen? Wir sind da.</span>
                        <span class="w-10 h-10 rounded-full bg-white dark:bg-slate-800 flex items-center justify-center shadow-sm">
                            <i class="fa-solid fa-chevron-right text-rose-500 group-hover:translate-x-1 transition-transform"></i>
                        </span>
                    </a>
                </div>
            </div>
        </div>
    </section>

    <style is:global>
        /* FORCE DARK MODE COLORS */
        html.dark .camp-card {
            background-color: #0f172a !important; /* slate-900 */
            border-color: rgba(255,255,255,0.1) !important;
        }
        html.dark .camp-title { color: #ffffff !important; }
        html.dark .camp-desc { color: #94a3b8 !important; }
        html.dark .camp-loc { color: #cbd5e1 !important; }
        html.dark .camp-link { color: #ffffff !important; }
    </style>

</MainLayout>